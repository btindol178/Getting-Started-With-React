# Get started with react guide
##################################
0) Remove service worker in index.js file
1) Go to google extensions install react developer extension
2) check if node is greater than 8 in cmd node -v
3) same thing with npm version use in cmd npm -v (needed to update) npm install -g npm@8.4.1  || Removed previous dependencies(yarn global remove create-react-app) || (npm uninstall -g create-react-app))
4) (First cd to where you want this to be stored) This will create project called react-essentials ( npx create-react-app react-essentials) ((to see more go to website reate-react-app)) || Another way to run react app type in browser ((react.new))
5) still within cmd change directory to the app (react-essentials) cd react-essentials
6) then in cmd run ((npm start)) (maybe need to hit enter a second time) 
7)  first cd .. out of cmd part of prject and go to  C:\Users\btindol\react-essentials> grab folder and open in  visual studio
8) in src index.js is app.py in python

npm install

#######################################
# Side Note
#######################################
  npm start
    Starts the development server.

  npm run build
    Bundles the app into static files for production.

  npm test
    Starts the test runner.

  npm run eject
    Removes this tool and copies build dependencies, configuration files
    and scripts into the app directory. If you do this, you can�t go back!

We suggest that you begin by typing:

  cd react-essentials
  npm start

#######################################
#######################################
# Useful tools
################
1) Babel
